# Backend Environment Variables (copy to .env)
NODE_ENV=development
PORT=4000

# Supabase Configuration
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_KEY=your_service_role_key_here

# JWT Secret (minimum 32 characters - generate a strong random key)
JWT_SECRET=your_super_secret_jwt_key_here_make_it_at_least_32_characters_long

# Production values:
# NODE_ENV=production
# PORT=8080 (for Railway deployment)

# JWT Secret generation methods:
# Option 1: openssl rand -base64 32
# Option 2: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Option 3: https://generate-random.org/api-key-generator